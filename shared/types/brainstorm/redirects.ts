import { WorkflowPages, type WorkflowPage } from "../../config/workflow";

// Redirects are workflow pages that the backend authorizes navigation to
export const Redirects = WorkflowPages;
export type RedirectType = WorkflowPage;