; Fail (don't just warn) when Node is older than package.json "engines".
; rolldown/tsdown import node:util styleText, added in Node 20.12 — on
; older Node the build dies with a cryptic SyntaxError instead.
engine-strict=true
