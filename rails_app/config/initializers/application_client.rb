# frozen_string_literal: true

# ApplicationClient configuration can be added here if needed
# The base ApplicationClient from Jumpstart Pro doesn't use ActiveSupport::Configurable
# Configure timeouts directly in subclasses if needed
