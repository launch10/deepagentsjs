# Playwright/E2E-specific overrides (layered on top of .env.test)
# These settings are NOT used by RSpec — only by bin/dev-test for Playwright runs.

# Workers run in background threads within the Rails process (no separate Sidekiq).
# This allows the deploy graph to yield at "wait" points while workers complete.
SIDEKIQ_THREADED=true
